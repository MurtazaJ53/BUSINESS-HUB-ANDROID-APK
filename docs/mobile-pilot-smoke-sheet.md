# Mobile Pilot Smoke Sheet

## Purpose

This sheet is the floor-ready checklist for a real pilot-device handoff.

Use it after the signed APK is built and before a shop is allowed to treat the Flutter app as a trusted working surface.

## Release identity to record

- release tag:
- pilot scope:
- version + build:
- release channel:
- short release SHA:
- APK SHA256:
- shop:
- operator:
- device model:
- Android version:
- install date/time:

## In-app snapshot capture

1. Install the signed APK from the mobile release workflow output.
2. Open:
   - [D:/business-hub/apps/mobile_flutter/lib/features/settings/presentation/settings_screen.dart](D:/business-hub/apps/mobile_flutter/lib/features/settings/presentation/settings_screen.dart)
3. Confirm `Pilot readiness signoff` is not `BLOCKED STARTUP`.
4. Run `Run smoke checklist` and mark the floor checks from the real device.
5. Use `Copy smoke report`.
6. Use `Copy pilot snapshot`.
7. Paste the smoke report and snapshot into:
   - the rollout thread, or
   - the release ticket, or
   - the QA signoff log

## Checksum verification

1. Confirm the installed APK came from the intended GitHub release artifact.
2. Compare the APK against:
   - `.sha256` file from the release lane
   - release manifest generated by [D:/business-hub/.github/workflows/flutter_mobile_release.yml](D:/business-hub/.github/workflows/flutter_mobile_release.yml)
3. If the checksum does not match:
   - stop the pilot
   - reinstall the correct APK

## Device smoke checklist

| Check | Result | Notes |
| --- | --- | --- |
| Owner login works | Pass / Fail |  |
| Staff login works | Pass / Fail |  |
| Inventory search works | Pass / Fail |  |
| Barcode / scanner flow works | Pass / Fail |  |
| Cash sale works | Pass / Fail |  |
| Split payment sale works | Pass / Fail |  |
| Partial / due sale works | Pass / Fail |  |
| Customer attach in checkout works | Pass / Fail |  |
| Customer ledger payment works | Pass / Fail |  |
| History receipt detail works | Pass / Fail |  |
| Settings build identity matches release | Pass / Fail |  |
| Outbox replay succeeds after reconnect | Pass / Fail |  |

## Pilot acceptance rule

Accept the pilot device only if:

- build identity matches the intended release
- APK checksum matches release output
- readiness signoff is not `BLOCKED STARTUP`
- all critical smoke checks pass
- copied smoke report is archived with the release record
- copied pilot snapshot is archived with the release record

## Blockers that require rollback

- wrong version/build or wrong release SHA
- checksum mismatch
- checkout failure
- payment replay failure
- customer credit flow corruption
- persistent outbox failure after reconnect

## If a smoke step fails

1. Open the in-app `Recovery desk`.
2. Retry the affected receipt or all attention items.
3. Copy the recovery report.
4. Attach the recovery report to the rollout/support thread.
5. Re-check [D:/business-hub/docs/mobile-pilot-readiness-signoff.md](D:/business-hub/docs/mobile-pilot-readiness-signoff.md) before allowing the device back into shift.
6. Use [D:/business-hub/docs/mobile-pilot-recovery-playbook.md](D:/business-hub/docs/mobile-pilot-recovery-playbook.md) to decide whether the device can stay in pilot.
